services:
  app:
    build: 
      context: .
      dockerfile: Dockerfile
    container_name: codebase_maintainer_app
    ports:
      - "4000:4000"
    env_file:
      - .env
    volumes:
      - .:/app
      - /app/node_modules
